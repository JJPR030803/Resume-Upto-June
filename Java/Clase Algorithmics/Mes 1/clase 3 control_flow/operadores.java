public class operadores {

    public static void main(String[] args) {
        /***
         * == igual que
         * != diferente que
         * < menor que
         * > mayor que
         * => igual mayor que
         * =< igual o menor que
         * && operador and
         * || operador or
         * 
         */
    }
}